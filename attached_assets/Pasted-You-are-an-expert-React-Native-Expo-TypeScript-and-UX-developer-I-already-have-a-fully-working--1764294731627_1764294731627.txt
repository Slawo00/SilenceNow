You are an expert React Native, Expo, TypeScript and UX developer.  
I already have a fully working Expo app called "SigmaFinanceAI".  
Your task is to EXTEND and IMPROVE the app WITHOUT rebuilding it.

Do NOT remove or overwrite existing features.  
Do NOT redesign the navigation structure.  
Only add enhancements, fix missing UI consistency, and improve UX.

============================================================
GLOBAL HEADER REQUIREMENT (IMPORTANT)
============================================================
- Every screen must use a consistent top header.
- Top-right: hamburger menu icon.
- Top-left: SigmaFinanceAI logo.
- Pressing the hamburger icon opens a RIGHT-SIDE slide-in Drawer.
- Add missing headers to all screens.

============================================================
DRAWER MENU EXTENSION (SLIDE-IN FROM RIGHT)
============================================================
Implement a slide-in Drawer from the right side.

Drawer menu items (in this order):
- Home
- Goals
- My Plan
- AI Tools
- How it Works
- Data & Privacy
- Contact / Feedback
- Terms & Conditions
- Onboarding (restart onboarding)

Drawer follows dark theme and uses existing color palette.

============================================================
LEVER LIST — IMPROVED OVERVIEW CARD DESIGN
============================================================
On the Goal → Lever overview page:

Replace current lever cards with a compact 2-row CFO-optimized design:

Row 1:
  Lever Title (left aligned)

Row 2:
  Priority: [High]   Effort: [Moderate]   Impact: [High]

(Priority must now also be visible on the overview page — not only inside details.)

Make the card small, clean, highly readable.  
Keep the dark theme.

============================================================
LEVER DETAIL SCREEN — EXTENDED (MATCH MY SCREENSHOTS)
============================================================
Keep existing design, but ensure the following sections appear in this exact order:

1. Priority badge (“High Priority”, etc.)
2. Effort/Benefit badge
3. Description
4. Responsible Roles
5. Key KPIs
6. Challenges & Risks
7. Practical Examples
8. Technology Requirements
9. Change Management
10. AI Tools (chips)

AI Tool Chips:
- When tapped → navigate to the AI Tool Detail Screen
- Do not limit to 2 tools anymore

============================================================
AI TOOLS — IMPROVEMENTS (LOGOS + URL + FULL LIST)
============================================================
Extend each tool object with:

id: string
name: string
description: string
category: string
recommendedGoals: string[]
logo: require(”../assets/logos/.png”)
url: “https://tool-website.com”

Changes needed:

1. Display tool logos in the AI Tools list (left of name).
2. Add “Open Website” button in ToolDetailScreen (Linking.openURL).
3. Add tool logos inside ToolDetailScreen above the title.
4. When a lever shows AI Tools → clicking them navigates to ToolDetailScreen.
5. Populate each lever with the TOP relevant tools (not only 2).
6. Add filtering chips for each CFO goal:
   - Speed
   - Quality & Accuracy
   - Automation
   - Compliance

============================================================
HOME SCREEN — IMPROVE (EXTEND ONLY)
============================================================
Add:
- Big value headline: “Transforming Finance with AI & Automation”
- 3–4 benefit highlights
- CTA button “Get Started”
- Short “How it Works” section with 3 steps

If onboarding not complete → CTA starts onboarding  
Else → CTA navigates to Goals tab.

============================================================
ONBOARDING FLOW
============================================================
Add a simple onboarding flow shown only once:

Questions:
1) Primary optimization goal (Speed, Quality, Automation, Compliance)
2) Company Size (Small / Mid-size / Large)
3) Average close duration (days)

Store results in AsyncStorage under “onboardingCompleted” and “onboardingData”.

After completion:

- Navigate to Goals
- Show a banner:
  “Recommended starting goal: <goal>”

============================================================
“MY PLAN” TAB (NON-DESTRUCTIVE ADDITION)
============================================================
Create a new bottom-tab “My Plan”.

Add MyPlanContext storing:
- goalId
- goalTitle
- leverId
- leverTitle
- priority
- impact
- effort
- status (“Planned”, “In progress”, “Done”)

In Goal Detail Screen:
- Add button “Add all levers to My Plan”

In My Plan Screen:
- Group by goal
- Allow status change
- Persist with AsyncStorage

============================================================
GITHUB INTEGRATION
============================================================
Add:

A. .gitignore with:
- node_modules/
- .expo/
- .expo-shared/
- dist/
- build/
- .DS_Store
- android/
- ios/

B. README.md explaining:
- install
- expo start
- how to connect Replit to GitHub
- folder structure

============================================================
SUPABASE READINESS (OPTIONAL)
============================================================
Add file: /services/supabaseClient.ts

Contains:
- placeholder client (commented out)
- comments for future auth + cloud storage + user plans

Do NOT activate Supabase in the code.

============================================================
OUTPUT FORMAT
============================================================
Provide in response:

1. Full code for new components (only new files)
2. Changed lines only for existing components
3. Updated navigation with Drawer + Header consistency
4. Updated Home, AI Tools, ToolDetailScreen, LeverCard, LeverDetail
5. MyPlanContext file
6. Onboarding screens
7. README.md additions
8. .gitignore

IMPORTANT:
Do NOT break existing functionality.
Do NOT remove existing styles.
Only EXTEND the app.
Keep all screens running in Expo Go.